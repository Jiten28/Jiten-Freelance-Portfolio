import {useEffect,useRef,useState} from 'react';
export function CustomSelect({label,value,onChange,options}){const[open,setOpen]=useState(false);const ref=useRef(null);useEffect(()=>{const h=e=>{if(e.key==='Escape')setOpen(false);if(e.type==='pointerdown'&&ref.current&&!ref.current.contains(e.target))setOpen(false)};addEventListener('pointerdown',h);addEventListener('keydown',h);return()=>{removeEventListener('pointerdown',h);removeEventListener('keydown',h)}},[]);return <div className="field custom-select" ref={ref}><label>{label}</label><button type="button" className="select-trigger" aria-haspopup="listbox" aria-expanded={open} onClick={()=>setOpen(!open)}>{value||'Select'}<span>⌄</span></button>{open&&<div className="select-menu" role="listbox">{options.map(o=><button key={o} type="button" role="option" aria-selected={value===o} onClick={()=>{onChange(o);setOpen(false)}}>{o}</button>)}</div>}</div>}
