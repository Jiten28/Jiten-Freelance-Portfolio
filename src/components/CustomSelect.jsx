import {useEffect,useRef,useState} from 'react';
export function CustomSelect({label,value,onChange,options}){const[open,setOpen]=useState(false);const ref=useRef(null);useEffect(()=>{const h=e=>{if(ref.current&&!ref.current.contains(e.target))setOpen(false);if(e.key==='Escape')setOpen(false)};addEventListener('pointerdown',h);addEventListener('keydown',h);return()=>{removeEventListener('pointerdown',h);removeEventListener('keydown',h)}},[]);const current=value||'';return <div className="field custom-select" ref={ref}><label>{label}</label><button type="button" className="select-trigger" aria-haspopup="listbox" aria-expanded={open} onClick={()=>setOpen(!open)}>{current||'Select'}<span>⌄</span></button>{open&&<div className="select-menu" role="listbox">{options.map(o=><button key={o} type="button" role="option" aria-selected={current===o} onClick={()=>{onChange(o);setOpen(false)}}>{o}</button>)}</div>}</div>}
